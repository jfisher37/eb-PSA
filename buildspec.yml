version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18.17.1  # Specify the desired Node.js version
    commands:
      - npm install -g npm@latest
  build:
    commands:
      # Your build commands here
